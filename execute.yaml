name: Partha

on:
  push:
    branches:
      - feature-demo
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Print Hello
        run: echo "Hello from feature-demo branch!"

      - name: Show Branch
        run: echo "Branch: ${{ github.ref_name }}"
